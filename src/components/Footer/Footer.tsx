
import { DarkModeToggle, ScrollArrows } from './UIControls';

const Footer = () => {
    return (
        <footer>
            <div>
                <DarkModeToggle />
            </div>

            <div>
                <ScrollArrows />
            </div>
        </footer>
    );
}

export default Footer;